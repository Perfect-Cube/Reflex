import React from 'react';
import InterviewUI from '../components/interview/InterviewUI';

const InterviewPage = () => {
    // This page can be minimal as the main logic is in the component
    return <InterviewUI />;
};

export default InterviewPage;